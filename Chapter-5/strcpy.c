/**
 * strcpy - copy strings.
 * @t: Source
 * @s: Destination
 * Return : Nothing.
 */
void strcpy(char *s, char *t)
{
	while(*s++ = *t++) /*copy char from t to s until it */
		;	   /* reached '/0'*/
			   /*the condition will be 0 so the loop will be stopped*/
		
}
